# cooking 项目协作规则

## 角色分工

- WorkBuddy：产品规划、任务拆解、文档汇总、阶段验收。
- CodeBuddy：代码实现、测试执行、问题修复、工程运行。
- 产品经理：维护 `01-requirements/` 和 `00-management/` 中的需求文件。
- 前端开发：负责现有 `src/` 前端代码及前端相关测试。
- 后端开发：负责后端代码、数据模型和 API；当前仓库若尚无后端，先提交技术方案，不得擅自选型。
- 测试：维护 `05-testing/`，根据验收标准编写和执行测试。
- 运维：维护 `06-devops/` 和 `07-delivery/`，负责启动、构建、部署和回滚说明。

## 当前技术基线（已确认，不得擅自更改）

- C 端：移动端 H5 网页，Vue 2 + Vant 2，免登录。
- 后台：PC Web，Vue 2 + Element UI 2，需登录。
- 后端：Node.js + Express 4 + Sequelize 6 + MySQL 8。
- 接口字段以 `02-design/api-design.md` 为准，字段冻结；如需变更必须更新文档并同步 `decisions.md`。
- 推荐数量固定 4 个菜；C 端必须提供「总菜单」页支持筛选、加入与删除。

## 当前阶段

需求 v1.0 已冻结，技术设计 v1.0 已产出，等待用户评审；评审通过后进入工程初始化。

## 在 CodeBuddy 中协作

- 工作区根目录必须是 `E:/AICode/cooking`，CodeBuddy 会自动加载 `CODEBUDDY.md` 与 `.codebuddy/rules/`。
- 角色定义在 `.codebuddy/agents/`：`backend-developer`、`frontend-developer`、`tester`、`devops`。
- 操作步骤与指令模板见 `docs/codebuddy-guide.md`。

## 工作规则

1. 修改代码前先阅读 `00-management/`、`01-requirements/` 和 `02-design/` 中的相关文件。
2. 不确定的业务规则必须标记为“待确认”，不得自行假设。
3. 不得擅自修改已经确认的 API、数据库字段和验收标准。
4. 每个任务完成后更新 `00-management/status/` 下对应角色的状态文件；并行开发时不要直接改 `00-management/task-board.md`（多会话会互相覆盖），由项目负责人统一合并。
5. 每次交付必须说明：完成内容、修改文件、验证命令、验证结果、未解决问题。
6. 发现其他模块问题时先记录问题，不要越权大范围修改。
7. 不得提交真实密码、Token、私钥或生产环境配置；敏感配置只放在本地环境变量中。
8. 不得为了通过测试而删除测试或降低验收标准。
9. 先打通一条最小完整业务链路，再扩展其他功能。

## 任务状态

使用以下状态：`待开始`、`进行中`、`待验收`、`已完成`、`阻塞`。

## 交接格式

每个任务至少包含：任务编号、目标、输入资料、完成范围、不包括范围、验收标准、依赖、交付物。
