# 待确认问题清单（已答复）

> 最后更新：2026-09-24
> 状态：13 项原始问题全部有结论，无阻塞项。后续如需求变更，在本文件追加新问题并同步 `decisions.md`。

## 一、用户已明确答复（2026-09-24）

| 编号 | 问题 | 结论 | 状态 |
| --- | --- | --- | --- |
| Q-01 | 「小程序」的形态 | **移动端 H5 网页**（手机浏览器 / 微信内置浏览器打开），不是微信小程序原生 | 已确认 |
| Q-02 | 是否需要账号体系 | **C 端免登录**，打开即用；管理后台需要管理员账号登录 | 已确认 |
| Q-03 | 推荐规则 | 规则推荐：排除最近 7 天已点菜品 + 分类分散，同一分类最多 2 个 | 已确认（默认方案） |
| Q-04 | 一份推荐几个菜 | **推荐 4 个菜** | 已确认 |
| Q-10 | 后端框架与数据库 | **Node.js + Express + MySQL**；ORM 用 Sequelize | 已确认 |
| Q-14 | 推荐不满意怎么办 | 去「总菜单」页按分类 / 关键词筛选，自行加入或删除菜品（新增需求） | 已确认 |

## 二、产品经理已定默认方案（用户未反对即按此执行，后续可调整）

| 编号 | 问题 | 采用方案 |
| --- | --- | --- |
| Q-05 | 菜品字段 | 名称、分类、主图、描述、食材、辣度（选填）、烹饪时长（选填）、状态、排序。**不做价格**（无支付与计价需求） |
| Q-06 | 食材使用方式 | 详情页展示 + 支持按食材名称搜索菜品；不做采购清单（后续版本） |
| Q-07 | 后台多管理员与权限 | 单管理员账号，不做角色权限 |
| Q-08 | 图片存储方式 | 后端本地磁盘存储 + 静态目录访问；数据库只存相对路径，便于后续切换对象存储 |
| Q-09 | 图片规格 | jpg / png / webp，单张 ≤ 5MB，后端生成缩略图（列表用缩略图） |
| Q-11 | 部署环境 | 第一版本机 / 局域网单机部署，提供 Docker Compose 一键启动 |
| Q-12 | HTTPS 与域名 | 第一版 HTTP 即可；对外发布时再补 HTTPS 与域名 |
| Q-13 | 种子数据 | 提供约 30 道常见家常菜种子数据（含分类、描述、食材，图片用占位图） |

## 三、仍然保留的不确定性（不阻塞开发，实现时按下列方式处理）

| 编号 | 问题 | 处理方式 |
| --- | --- | --- |
| Q-15 | 同一天推荐结果是否需要固定 | 同一天首次进入结果固定（以日期为随机种子），点「换一批」后变化 |
| Q-16 | 菜品是否允许重名 | 允许重名，但新增时若同名给出提示 |
| Q-17 | 分类下仍有菜品时能否删除分类 | 允许删除，需二次确认并提示「该分类下 N 个菜品将变为未分类」 |
| Q-18 | 删除菜品是物理还是逻辑删除 | 逻辑删除（`deleted_at`），历史菜单保留菜名与图片快照 |
